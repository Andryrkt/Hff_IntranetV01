<div class="email-header">
	{{ numDa }}
	- DEMANDE D'APPROVISIONNEMENT
	<span class="{{ statut }}">
		{% if statut == "validationDa" %}
			VALIDÉ
		{% elseif statut == "newDa" %}
			CRÉÉ
		{% endif %}
	</span>
</div>
