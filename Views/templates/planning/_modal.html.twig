<div class="modal fade" tabindex="-1" id="listeCommande" tabindex="-1" aria-labelledby="modalLabel" aria-hidden="true">
	<div class="modal-dialog modal-dialog-scrollable modal-xl">
		<div class="modal-content">
			<div class="modal-header">
				<h5 class="modal-title">
					Listes ligne de l'OR
					<small class="text-success d-block mt-auto" id="orIntv"></small>
				</h5>

				<div class="ms-auto d-flex align-items-center">
					<div id="datePlanningCommentaire"></div>
					<a id="dossierDitLink" class="btn btn-warning fw-bold" target="_blank" href="#">Dossier DIT</a>
					<button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
				</div>
			</div>
			{# body du modal#}
			<div
				class="modal-body">
				<!-- Champ avec légende -->

				<div class="mb-3">
					<small style="color: #c8ad7f">DISPO STOCK |</small>
					<small class="text-danger">Back Order / Error |</small>
					<small style="color: #9ACD32">Commande envoyé CAT |</small>
					<small class="text-warning">Réception Partielle</small>
				</div>
				<!-- Spinner de chargement -->
				<div id="loading" class="text-center">
					<div class="spinner-border" role="status">
						<span class="visually-hidden">Chargement...</span>
					</div>
				</div>

				<!-- Contenu des données -->
				<div id="dataContent" style="display: none;">

					<table class="table rounded mt-1">
						<thead class="table">
							<tr
								id="planningTableHead">{# <th>N° OR</th>
																	<th>Intv</th>
																	<th>N° Commande</th>
																	<th>Statut ctrmrq</th>
																	<th>CST</th>
																	<th>Ref</th>
																	<th>Désignation</th>
																	<th>Qté OR</th>
																	<th>Qté ALL</th>
																	<th>QTé RLQ</th>
																	<th>QTé LIV</th>
																	<th>Statut</th>
																	<th>Date Statut</th>
																	<th>ETA Ivato</th>
																	<th>ETA Magasin</th>
																	<th>Message</th> #}
							</tr>
						</thead>
						<tbody id="commandesTableBody">{# {% for command in commandes %} #}
							{# <tr> #}
							{# <td>{{ command.slor_numcf}}</td> #}
							{# <td>{{ command.fcde_date}}</td> #}
							{# <td>--</td> #}

							{# </tr> #}
							{# {% endfor %} #}
						</tbody>
					</table>

					{# AFFICHAGE Technicients Intervenant #}
					<h3 class="fw-bold bg-warning p-2">Technicien(s) interventant(s)</h3>
					<table class="table rounded mt-1">
						<thead class="table">
							<tr>
								<th>Matricule</th>
								<th>Nom et prenom(s)</th>
							</tr>
						</thead>
						<tbody id="technicienTableBody"></tbody>
					</table>
				</div>
			</div>
		</div>
	</div>
</div>
