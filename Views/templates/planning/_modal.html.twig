<div class="modal fade" tabindex="-1" id="listeCommande" tabindex="-1" aria-labelledby="modalLabel" aria-hidden="true">
	<div class="modal-dialog modal-dialog-scrollable modal-xl">
		<div class="modal-content">
			<div class="modal-header">
				<h5 class="modal-title">
					Listes lignes de l'intervention
					<small class="text-success d-block mt-auto" id="orIntv" style="font-size: 0.7em; font-weight: bold;"></small>
				</h5>

				<div class="ms-auto d-flex align-items-center">
					<a id="dossierDitLink" class="btn btn-warning fw-bold" target="_blank" href="#">Dossier DIT</a>
					<button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
				</div>
			</div>
			{# body du modal#}
			<div
				class="modal-body">
				<!-- Champ avec légende -->

				<div class="mb-3">
					<small style="color: #c8ad7f">DISPO STOCK |</small>
					<small class="text-danger">Back Order / Error |</small>
					<small style="color: #9ACD32">Commande envoyé CAT |</small>
					<small class="text-warning">Réception Partielle</small>
				</div>
				<!-- Spinner de chargement -->
				<div id="loading" class="text-center">
					<div class="spinner-border" role="status">
						<span class="visually-hidden">Chargement...</span>
					</div>
				</div>

				<!-- Contenu des données -->
				<div
					id="dataContent" style="display: none;">
					{# <div id="table-container-detail"></div> #}
					<table class="table rounded mt-1">
						<thead class="table">
							<tr id="planningTableHead"></tr>
							<tr id="planningTableHead"></tr>
						</thead>
						<tbody id="commandesTableBody"></tbody>
						<tbody id="commandesTableBody"></tbody>
					</table>

					{# AFFICHAGE Technicients Intervenant #}
					<h3 class="fw-bold bg-warning p-2 technicien">
						Technicien(s) interventant(s)</h3>
					<div id="table-container"></div>
				</div>
			</div>
		</div>
	</div>
</div>
